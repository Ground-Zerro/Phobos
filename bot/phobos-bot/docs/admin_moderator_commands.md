# Команды администраторов и модераторов

## Обзор

Phobos Bot предоставляет два уровня привилегированных пользователей:
- **Moderator** (модератор) - расширенные права для управления пользователями и просмотра логов
- **Admin** (администратор) - полные права управления системой

## Команды для модераторов (Moderator)

Модераторы имеют доступ к следующим командам для мониторинга и поддержки пользователей.

### /logs
**Просмотр логов событий**

```
/logs
```

**Описание:**
- Отображает последние события в системе
- Включает действия пользователей, ошибки, выполнение команд
- Помогает диагностировать проблемы пользователей

**Формат вывода:**
```
📋 Логи событий (последние записи):

🕐 2025-11-16 10:30:45
👤 Пользователь: @username (ID: 123456789)
📍 Команда: create
✅ Статус: успешно
📝 Вывод: Created configuration for client_name

🕐 2025-11-16 10:29:12
👤 Пользователь: @another_user (ID: 987654321)
📍 Команда: feedback_sent
✅ Статус: успешно
⚠️ Ошибка: (если есть)
```

**Примечания:**
- Показывает последние N записей (настраивается в коде)
- Можно фильтровать по пользователю через параметры (если реализовано)

---

### /users
**Список зарегистрированных пользователей**

```
/users
```

**Описание:**
- Показывает всех пользователей, зарегистрированных в системе
- Включает информацию об уровне доступа, премиум-статусе, дате регистрации
- Полезно для мониторинга активности и поддержки

**Формат вывода:**
```
👥 Зарегистрированные пользователи:

1. @username (ID: 123456789)
   Уровень: basic
   Регистрация: 2025-11-01 15:30:00

2. @premium_user (ID: 987654321)
   Уровень: premium
   Истекает: 2025-12-01
   Регистрация: 2025-10-15 12:00:00

3. @admin_user (ID: 1458954246)
   Уровень: admin
   Регистрация: 2025-09-01 10:00:00
```

**Примечания:**
- Список может быть длинным, рекомендуется пагинация
- Показывает актуальный статус премиум-доступа

---

### /feedbacklist
**Список сообщений обратной связи**

```
/feedbacklist
```

**Описание:**
- Показывает все сообщения обратной связи от пользователей
- Включает обработанные и необработанные запросы
- Позволяет отслеживать запросы поддержки

**Формат вывода:**
```
📬 Список обратной связи:

🆕 ID: 15 | От: @username (ID: 123456789)
Время: 2025-11-16 10:45:00
Сообщение: Не могу создать конфигурацию, ошибка при выполнении скрипта
Статус: не обработано

✅ ID: 14 | От: @another_user (ID: 987654321)
Время: 2025-11-16 09:30:00
Сообщение: Спасибо за помощь!
Статус: обработано
Обработал: @admin_user
```

**Примечания:**
- Модераторы видят все сообщения
- Могут отвечать на feedback через inline-кнопки (см. раздел "Обработка обратной связи")

---

### Обработка обратной связи (inline-кнопки)

Когда пользователь отправляет сообщение через `/feedback`, модераторы получают уведомление с действиями:

**Для сообщений от обычных пользователей:**

Модераторы видят только кнопку:
- **✅ Прочитано** - отметить как прочитанное (информационная функция)

**Для сообщений от других модераторов/администраторов:**

Модераторы также видят только:
- **✅ Прочитано** - ознакомлены с информацией

**Примечания:**
- Модераторы НЕ могут изменять уровни пользователей
- Модераторы НЕ могут удалять конфигурации других пользователей
- Модераторы могут отвечать пользователям (если предусмотрено)

---

## Команды для администраторов (Admin)

Администраторы имеют доступ ко всем командам модераторов ПЛЮС следующие команды управления.

### Команды модераторов
Администраторы имеют доступ ко всем командам модераторов:
- `/logs` - просмотр логов
- `/users` - список пользователей
- `/feedbacklist` - список обратной связи

---

### /setlevel
**Изменение уровня доступа пользователя**

```
/setlevel <user_id> <level>
```

**Параметры:**
- `user_id` - ID пользователя Telegram (число)
- `level` - новый уровень: `basic`, `premium`, `moderator`, `admin`

**Примеры:**
```bash
# Назначить пользователя модератором
/setlevel 123456789 moderator

# Понизить до обычного пользователя
/setlevel 987654321 basic

# Назначить администратором
/setlevel 1458954246 admin

# Установить премиум (рекомендуется использовать /setpremium)
/setlevel 555555555 premium
```

**Примечания:**
- Меняет значение `user_level` в таблице `users`
- Для премиум-доступа лучше использовать `/setpremium` (устанавливает срок действия)
- Администраторы и модераторы автоматически получают все премиум-привилегии
- Изменения применяются немедленно

**Отличия уровней:**
- `basic` - обычный пользователь с ограничениями
- `premium` - премиум-пользователь без ограничений по времени
- `moderator` - доступ к командам мониторинга
- `admin` - полный доступ ко всем командам

---

### /setpremium
**Установка премиум-статуса с датой истечения**

```
/setpremium <user_id> <days|permanent> [reason]
```

**Параметры:**
- `user_id` - ID пользователя Telegram
- `days` - количество дней действия премиума (число)
- `permanent` - бессрочный премиум (ключевое слово)
- `reason` - причина предоставления (опционально, текст)

**Примеры:**
```bash
# Предоставить премиум на 30 дней
/setpremium 123456789 30

# Предоставить бессрочный премиум
/setpremium 987654321 permanent

# С указанием причины
/setpremium 123456789 90 Оплата за 3 месяца

# Бессрочный с причиной
/setpremium 555555555 permanent Спонсор проекта
```

**Функционал:**
- Устанавливает `user_level = 'premium'`
- Заполняет `premium_expires_at` (NULL для permanent)
- Сохраняет причину в `premium_reason`
- Обновляет `updated_at`

**Примечания:**
- НЕ меняет уровень, если пользователь уже admin или moderator
- Для продления просто выполните команду снова с новым периодом
- Срок истечения рассчитывается от текущей даты, а не от предыдущего срока

**Автоматическая проверка:**
- Система автоматически проверяет истечение премиума при каждом запросе
- Истекший премиум не дает привилегий

---

### /block
**Блокировка пользователя**

```
/block <user_id> <reason>
```

**Параметры:**
- `user_id` - ID пользователя Telegram
- `reason` - причина блокировки (обязательно, текст)

**Примеры:**
```bash
# Блокировка за нарушение
/block 123456789 Спам в feedback

# Блокировка с подробной причиной
/block 987654321 Злоупотребление ресурсами сервера, создание множественных аккаунтов
```

**Функционал:**
- Добавляет запись в таблицу `blocklist`
- Заблокированный пользователь не может использовать бота
- Все команды от заблокированного пользователя игнорируются
- Сохраняется информация о блокировке (кто, когда, почему)

**Примечания:**
- Блокировка применяется немедленно
- Заблокированный пользователь получает сообщение о блокировке при попытке использования
- История блокировок сохраняется в логах

---

### /unblock
**Разблокировка пользователя**

```
/unblock <user_id>
```

**Параметры:**
- `user_id` - ID пользователя Telegram

**Примеры:**
```bash
# Разблокировать пользователя
/unblock 123456789
```

**Функционал:**
- Удаляет пользователя из `blocklist`
- Пользователь снова может использовать бота
- Логируется событие разблокировки

**Примечания:**
- Если пользователь не был заблокирован, выдается соответствующее сообщение
- Разблокировка применяется немедленно

---

### /config
**Управление конфигурацией системы**

```
/config <key> [value]
```

**Параметры:**
- `key` - имя параметра конфигурации
- `value` - новое значение (если не указано, показывает текущее)

**Примеры:**
```bash
# Просмотр значения
/config log_level

# Изменение значения
/config log_level DEBUG

# Другие параметры
/config max_clients 100
/config max_test_duration 72h
/config watchdog_inactive_threshold 24h
```

**Доступные параметры:**
- `log_level` - уровень логирования (DEBUG, INFO, WARNING, ERROR)
- `max_clients` - максимум одновременных подключений
- `max_test_duration` - время тестового периода
- `watchdog_inactive_threshold` - порог неактивности для удаления
- И другие параметры из таблицы `configuration`

**Примечания:**
- Изменения применяются немедленно (или после перезагрузки, зависит от параметра)
- ConfigReloader автоматически обновляет конфигурацию каждые 5 минут
- Некоторые параметры требуют перезапуска бота

---

### /stats
**Статистика системы**

```
/stats
```

**Описание:**
- Показывает общую статистику работы бота
- Информация о пользователях, конфигурациях, обратной связи
- Данные о производительности и ресурсах

**Формат вывода:**
```
📊 Статистика системы

Пользователи:
• Всего: 150
• Basic: 120
• Premium: 25
• Moderators: 3
• Admins: 2

Конфигурации:
• Активные: 85
• Всего: 150

Обратная связь:
• Необработанная: 5
• Всего: 250

Система:
• Размер БД: 15.3 MB
• Uptime: 5 days, 12 hours
```

**Примечания:**
- Данные обновляются в реальном времени
- Полезно для мониторинга нагрузки
- Помогает планировать ресурсы

---

### /cleanuplogs
**Очистка старых логов**

```
/cleanuplogs <days>
```

**Параметры:**
- `days` - удалить логи старше указанного количества дней

**Примеры:**
```bash
# Удалить логи старше 30 дней
/cleanuplogs 30

# Удалить логи старше 7 дней
/cleanuplogs 7

# Удалить логи старше 90 дней
/cleanuplogs 90
```

**Функционал:**
- Удаляет записи из таблицы `logs` старше указанного срока
- Освобождает место в базе данных
- Требует подтверждения перед удалением
- Показывает количество удаленных записей

**Примечания:**
- Операция необратима
- Рекомендуется делать резервную копию перед очисткой
- Система запросит подтверждение
- Регулярная очистка помогает поддерживать производительность

---

### /backup
**Управление резервными копиями**

```
/backup [create|list|restore <filename>]
```

**Команды:**

**Создать резервную копию:**
```
/backup create
```
- Создает сжатую копию базы данных
- Сохраняет в директорию `backups/`
- Имя файла: `phobos-bot-YYYY-MM-DD-HH-MM-SS.db.gz`

**Список копий:**
```
/backup list
```
- Показывает все доступные резервные копии
- Включает размер файла и дату создания

**Восстановление:**
```
/backup restore phobos-bot-2025-11-15-10-30-00.db.gz
```
- Восстанавливает базу данных из указанной копии
- Требует подтверждения
- Автоматически создает копию текущей БД перед восстановлением

**Примечания:**
- Автоматические копии создаются каждые 24 часа
- Старые копии автоматически удаляются (retention: 7 дней)
- Восстановление требует перезапуска бота
- Критически важно для disaster recovery

---

### Обработка обратной связи для администраторов

Когда пользователь отправляет feedback, администраторы получают уведомление с полным набором действий:

**Для сообщений от обычных пользователей:**

- **💬 Ответить** - отправить ответ пользователю
- **👤 Сменить уровень** - изменить user_level (basic/premium/moderator)
- **🗑 Удалить конфиг** - удалить конфигурацию пользователя
- **✅ Прочитано** - отметить как обработанное

**Для сообщений от модераторов/администраторов:**

- **✅ Прочитано** - информационное сообщение

**Процесс ответа:**
1. Нажать "💬 Ответить"
2. Бот попросит ввести текст ответа
3. Ввести текст
4. Ответ отправляется пользователю
5. Feedback помечается как обработанный

**Процесс смены уровня:**
1. Нажать "👤 Сменить уровень"
2. Выбрать новый уровень (basic/premium)
3. Указать срок действия (для premium)
4. Подтверждение

---

## Общие рекомендации

### Безопасность
- Не делитесь ID администраторов с непроверенными лицами
- Регулярно проверяйте список пользователей
- Мониторьте логи на подозрительную активность
- Используйте `/block` при обнаружении нарушений

### Мониторинг
- Проверяйте `/stats` ежедневно
- Просматривайте `/logs` при возникновении проблем
- Отвечайте на `/feedbacklist` оперативно
- Используйте уровень логирования INFO для production

### Резервное копирование
- Создавайте ручные копии перед важными изменениями
- Проверяйте наличие автоматических копий
- Храните критические копии отдельно
- Периодически тестируйте восстановление

### Управление пользователями
- Используйте `/setpremium` вместо `/setlevel premium`
- Указывайте причину при блокировке
- Документируйте изменения уровней доступа
- Регулярно проверяйте истекшие премиум-аккаунты

---

## Различия между Moderator и Admin

| Функция | Moderator | Admin |
|---------|-----------|-------|
| Просмотр логов (/logs) | ✅ | ✅ |
| Список пользователей (/users) | ✅ | ✅ |
| Обратная связь (/feedbacklist) | ✅ | ✅ |
| Ответ на feedback | ✅ (ограничено) | ✅ |
| Изменение уровня (/setlevel) | ❌ | ✅ |
| Премиум-статус (/setpremium) | ❌ | ✅ |
| Блокировка (/block) | ❌ | ✅ |
| Разблокировка (/unblock) | ❌ | ✅ |
| Конфигурация (/config) | ❌ | ✅ |
| Статистика (/stats) | ❌ | ✅ |
| Очистка логов (/cleanuplogs) | ❌ | ✅ |
| Резервные копии (/backup) | ❌ | ✅ |
| Удаление конфигураций | ❌ | ✅ |
| Обход ограничений | ✅ | ✅ |
| Защита от watchdog | ✅ | ✅ |

---

## Примеры сценариев использования

### Сценарий 1: Новый премиум-пользователь
```bash
# Пользователь оплатил 30 дней
/setpremium 123456789 30 Оплата через PayPal, #invoice-12345

# Проверить статус
/users
```

### Сценарий 2: Продление премиума
```bash
# Продлить еще на 30 дней (с текущей даты!)
/setpremium 123456789 30 Продление, оплата #invoice-67890
```

### Сценарий 3: Назначение модератора
```bash
# Повысить доверенного пользователя
/setlevel 987654321 moderator

# Проверить в списке
/users
```

### Сценарий 4: Обработка спама
```bash
# Заблокировать нарушителя
/block 555555555 Массовая рассылка спама через feedback

# Проверить логи
/logs
```

### Сценарий 5: Диагностика проблемы
```bash
# Включить детальное логирование
/config log_level DEBUG

# Воспроизвести проблему
# Посмотреть логи
/logs

# Вернуть обратно
/config log_level INFO
```

### Сценарий 6: Регулярное обслуживание
```bash
# Проверить статистику
/stats

# Создать резервную копию
/backup create

# Очистить старые логи
/cleanuplogs 90

# Проверить список feedback
/feedbacklist
```
